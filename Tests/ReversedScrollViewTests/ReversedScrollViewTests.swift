import XCTest
@testable import ReversedScrollView

final class ReversedScrollViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ReversedScrollView().text, "Hello, World!")
    }
}
